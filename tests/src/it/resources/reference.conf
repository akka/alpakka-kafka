akka {
  loglevel = "DEBUG"

  actor {
    serialize-messages = on
    debug {
      lifecycle = off
      receive = off
    }
  }

  test {
    single-expect-default = 10s
  }
  kafka.consumer {
    max-wakeups = 10
  }
}
